# ═══════════════════════════════════════════════════════════
# .gitignore — Git 版本控制忽略规则
# ═══════════════════════════════════════════════════════════
#
# 【知识点：.gitignore 的作用】
# Git 是分布式版本控制系统，所有提交的文件都会永久记录在历史中。
# .gitignore 告诉 Git "哪些文件不需要跟踪"，防止：
#   1. 敏感信息泄露（API Key、密码）
#   2. 无意义的大文件（编译产物、缓存）
#   3. 环境相关的本地配置（不同开发者的路径不同）
#
# 【语法规则】
#   - 每行一个模式
#   - # 开头是注释
#   - * 匹配任意字符（不含 /）
#   - ** 匹配任意层级目录
#   - ! 取反（排除忽略）
#   - / 开头表示仅匹配根目录
#
# 【重要原则】
#   如果文件已经被 Git 跟踪过，修改 .gitignore 不会生效！
#   需要先执行：git rm --cached <file> 取消跟踪。
#
# ═══════════════════════════════════════════════════════════

# ─────────────── 环境变量文件（含敏感密钥）───────────────
# 只保留 .env.example 作为模板供团队参考
.env
.env.*
!.env.example

# ─────────────── Python 运行时 ───────────────
__pycache__/
*.py[cod]
*$py.class
*.so

# ─────────────── 虚拟环境 ───────────────
.venv/
venv/
env/
ENV/

# ─────────────── 包管理与构建产物 ───────────────
dist/
build/
*.egg-info/
*.egg
.eggs/

# ─────────────── IDE / 编辑器 ───────────────
.idea/
.vscode/
*.swp
*.swo
*~

# ─────────────── 操作系统 ───────────────
.DS_Store
Thumbs.db
desktop.ini

# ─────────────── 测试 / 覆盖率 ───────────────
.pytest_cache/
.coverage
htmlcov/
.tox/

# ─────────────── 日志 ───────────────
*.log
logs/

# ─────────────── 类型检查缓存 ───────────────
.mypy_cache/
.pytype/
